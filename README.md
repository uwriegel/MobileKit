# MobileKit
## build
initial: 
```npm i```

build lib:
```ng build umobilekit```

publish lib:
* increment version number in package.json
```
ng build umobilekit --prod
cd dist/umobilekit
npm publish
```
```ng build umobilekit --prod```

